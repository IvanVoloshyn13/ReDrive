<resources>
    <!-- Base application theme. -->
    <style name="Base.V26.Theme.ReDrive" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimaryContainer">@color/md_theme_primaryContainer</item>

        <item name="colorOnPrimaryContainer">@color/md_theme_onPrimaryContainer</item>
        <item name="colorOnSurface">@color/md_theme_onPrimaryContainer</item>


        <!-- Customize your theme here. -->
    </style>

    <style name="Base.Theme.ReDrive" parent="Base.V26.Theme.ReDrive" />

    <style name="Theme.ReDrive" parent="Base.Theme.ReDrive">
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
    </style>

</resources>